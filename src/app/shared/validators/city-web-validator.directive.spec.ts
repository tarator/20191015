import { CityWebValidatorDirective } from './city-web-validator.directive';

describe('CityWebValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CityWebValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
